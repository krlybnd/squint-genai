# Presidio analyzer (PII detect). Profile: guardrails.
services:
  presidio-analyzer:
    profiles: ["guardrails"]
    image: ghcr.io/data-privacy-stack/presidio-analyzer:latest
    environment:
      WORKERS: "1"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:3000/health')\" || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s
