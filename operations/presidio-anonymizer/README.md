# Presidio anonymizer (PII redact)

Compose profile: **`guardrails`**. Internal DNS: `presidio-anonymizer:3000`.

```bash
make up-guardrails
```

Takes analyzer results and returns masked text. Cheap (~50 MiB RSS).
