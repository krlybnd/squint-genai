# Live service origins.
# make up (host ports):
API_BASE_URL=http://localhost:8000
CHAT_BASE_URL=http://localhost:8002
ADMIN_BASE_URL=http://localhost:8003
# make up-auth (Traefik :80 only):
# API_BASE_URL=http://localhost/api
# CHAT_BASE_URL=http://localhost/chat
# ADMIN_BASE_URL=http://localhost/admin-api

# Sent as X-API-Key when AUTH_MODE=api_key. Harmless when AUTH_MODE=none.
API_KEY=dev-admin-key-change-me
API_TENANT_ID=default

# Bound auth/isolation + GET /v1/me (features/07_auth.feature, features/07_me.feature) — password grant.
# Requires AUTH_MODE=jwt. Scenarios skip if the token endpoint is down
# or the API does not return 401/403 without a bearer.
KEYCLOAK_URL=http://localhost:8080
# make up-auth: KEYCLOAK_URL=http://localhost
KEYCLOAK_REALM=agentic-rag-eval
KEYCLOAK_TOKEN_CLIENT_ID=agentic-rag-eval-dev
# KEYCLOAK_TOKEN_URL=http://localhost:8080/realms/agentic-rag-eval/protocol/openid-connect/token

# Guardrails acceptance (features/05_guardrails.feature).
# Host ports: make up && make up-guardrails. Traefik: http://localhost/guard after up-auth + guardrails.
GUARD_API_BASE=http://localhost:8010
# GUARD_API_BASE=http://localhost/guard
GUARD_AUTH_TOKEN=poc-local-classifier
# Optional: skip indexed-documents lookup for comment scenarios
# API_TEST_CHUNK_ID=

# PII vault acceptance (features/06_pii_vault.feature)
# Requires indexing worker INDEXING_PDF_PII_TOKENIZATION_ENABLED=true and migration 003.
ANALYZER_API_BASE=http://localhost:5002
# make up-auth: ANALYZER_API_BASE=http://localhost/analyzer
# PII_VAULT_ENABLED=true
# PII_VAULT_TESTS_ENABLED=true
