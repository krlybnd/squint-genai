{
  "name": "squint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/ui-core",
    "frontend/app-ui",
    "frontend/admin-app-ui",
    "tests/e2e",
    "tests/api",
    "tools/demo-studio"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript-eslint": "^8.18.0"
  }
}
